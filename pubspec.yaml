name: mobile_api
description: "mobile api package for moam digital system"
version: 0.0.2
homepage: https://github.com/moamlogistics/mobile_api
repository: https://github.com/moamlogistics/mobile_api

environment:
  sdk: ^3.9.0
  flutter: ">=1.17.0"

dependencies:
  connectivity_plus: ^6.1.5
  flutter:
    sdk: flutter
  flutter_secure_storage: ^9.2.4
  fresh_graphql: ^0.6.1
  graphql: ^5.2.1
  http: ^1.5.0
  http_parser: ^4.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mocktail: ^1.0.4

 

# The following section is specific to Flutter packages.
flutter:

 